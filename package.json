{
	"name": "vscode-path-picker",
	"displayName": "Path Picker",
	"description": "Pick a file or folder like the File Picker, then copy its relative path (Enter), real path (Shift+Enter), or reveal it in the Explorer (Cmd+Enter).",
	"version": "0.0.1",
	"publisher": "lamnt45",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/lamnguyenx/vscode-path-picker.git"
	},
	"icon": "media/icon.png",
	"engines": {
		"vscode": "^1.100.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:pathPicker.pickPath",
		"onCommand:pathPicker.copyRealPath",
		"onCommand:pathPicker.revealInExplorer"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "pathPicker.pickPath",
				"title": "Pick File or Folder and Copy Path",
				"category": "Path Picker"
			},
			{
				"command": "pathPicker.copyRealPath",
				"title": "Copy Real Path",
				"category": "Path Picker"
			},
			{
				"command": "pathPicker.revealInExplorer",
				"title": "Reveal in Explorer",
				"category": "Path Picker"
			}
		],
		"keybindings": [
			{
				"command": "pathPicker.pickPath",
				"key": "meta+alt+shift+p",
				"mac": "ctrl+alt+shift+p",
				"linux": "meta+alt+shift+p",
				"win": "meta+alt+shift+p"
			},
			{
				"command": "pathPicker.copyRealPath",
				"key": "shift+enter",
				"when": "pathPickerPickerVisible"
			},
			{
				"command": "pathPicker.revealInExplorer",
				"key": "meta+enter",
				"mac": "meta+enter",
				"win": "ctrl+enter",
				"linux": "ctrl+enter",
				"when": "pathPickerPickerVisible"
			}
		],
		"menus": {
			"commandPalette": [
				{
					"command": "pathPicker.copyRealPath",
					"when": "false"
				},
				{
					"command": "pathPicker.revealInExplorer",
					"when": "false"
				}
			]
		},
		"configuration": {
			"title": "Path Picker",
			"properties": {
				"pathPicker.exclude": {
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"node_modules",
						".git",
						".hg",
						".svn"
					],
					"description": "Directory names that are skipped while indexing folders."
				},
				"pathPicker.maxEntries": {
					"type": "number",
					"default": 30000,
					"description": "Maximum number of files and folders to index."
				},
				"pathPicker.followSymlinks": {
					"type": "boolean",
					"default": false,
					"description": "Whether to follow symbolic links while indexing files and folders. Symlink cycles are detected and skipped."
				}
			}
		}
	},
	"scripts": {
		"compile": "tsc -p ./",
		"watch": "tsc -w -p ./",
		"vscode:prepublish": "npm run compile",
		"package": "vsce package"
	},
	"devDependencies": {
		"@types/node": "^26.0.0",
		"@types/vscode": "^1.100.0",
		"@vscode/vsce": "^3.9.2",
		"typescript": "^5.5.0"
	},
	"dependencies": {
		"ignore": "^7.0.6"
	}
}
